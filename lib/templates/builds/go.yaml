# Go build configuration
language: go
version: "1.23"
cmd: "go build -o ./dist"
resources:
  memory: 1024 # 1 GB — Go compiler + linker
env_vars:
  CGO_ENABLED: "0"
  GOOS: linux
  GOARCH: amd64
flags:
  ldflags:
    - "-s"
    - "-w"
    - "-extldflags '-static'"
