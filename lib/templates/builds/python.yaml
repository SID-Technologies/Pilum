# Python build configuration
language: python
version: "3.12"
cmd: "pip install -r requirements.txt"
resources:
  memory: 512 # 512 MB — pip install + wheel builds
env_vars:
  PYTHONDONTWRITEBYTECODE: "1"
  PYTHONUNBUFFERED: "1"
