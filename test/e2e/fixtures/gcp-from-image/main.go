// Stub for the gcp-from-image fixture. gcp-cloud-run-from-image's recipe
// has no build step, so this file isn't strictly required — but it lets
// Pilum's working-dir resolution treat the fixture as a valid service
// directory just like every other fixture in this suite.
package main

func main() {}
